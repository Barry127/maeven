---
title: Checkbox
path: Components/Form/Checkbox
---

import { Checkbox } from 'maeven';
import { Description, DoDont, Props } from 'docs/components';

# Checkbox

<Description of="Checkbox" />

## Props

<Props of="Checkbox" />

## Examples

### Basic

A basic usage of Checkbox.

```js live
<Checkbox>Hello World!</Checkbox>
```

### Controlled

A controlled Checkbox.

```js live withRender
const Controlled = props => {
  const [checked, setChecked] = useState(true);
  return (
    <Checkbox checked={checked} onChange={ev => setChecked(ev.target.checked)}>
      Toggle me
    </Checkbox>
  );
};

render(<Controlled />);
```

### Disabled

Checkboxes can be disabled.

```js live
<Checkbox indeterminate disabled>
  Disabled
</Checkbox>
```

### Size

Checkboxes can have different sizes.

```js live
<Row gutter={1}>
  <Col span={24}>
    <Checkbox size="sm">Small</Checkbox>
  </Col>
  <Col span={24}>
    <Checkbox size="md">Medium</Checkbox>
  </Col>
  <Col span={24}>
    <Checkbox size="lg">Large</Checkbox>
  </Col>
</Row>
```

### Error

A Checkbox can indicate it has an error.

```js live
<Checkbox hasError>Text indicating error</Checkbox>
```
