import { colors } from '../lib/theme/defaults';
import { PartialTheme } from '../types';

const theme: PartialTheme = {
  name: 'Maeven',
  colors: {
    primaryBackgroundText: colors.white,
    successBackgroundText: colors.white,
    warningBackgroundText: colors.white,
    dangerBackgroundText: colors.white
  }
};

export default theme;
